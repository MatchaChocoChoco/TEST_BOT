# TEST_BOT
## 環境構築
python で仮想環境を構築する場合は `python -m venv .venv` コマンドを実行します
### Activate
`source ./.venv/bin/activate` (Linux, Mac)
### pip (requirements.txt)
`pip install -r requirements.txt`で必要なパッケージをインストールできます